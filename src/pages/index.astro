---
import Layout from '../layouts/Layout.astro';
import CardCollection from '../components/CardCollection.svelte';

const { protocol, hostname, port } = Astro.url;
---

<Layout title="Where in the world?">
  <CardCollection {protocol} {hostname} {port} client:load />
</Layout>
